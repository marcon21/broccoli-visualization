# Demo Visualization for Broccoli Project

## Requirements

Run: `pip install -r requirements.txt`

## Usage

Run: `streamlit run app.py`

and open the link in the browser at `http://localhost:8501/`.
